# mcp_websearch
mcp_websearch: AI-powered web search + auto-summary via your own OpenAI-compatible API. Saves context, lightweight, free (API cost only). 通过Bing搜索并由您配置的AI自动总结，节省上下文，仅依赖requests，纯Python。
